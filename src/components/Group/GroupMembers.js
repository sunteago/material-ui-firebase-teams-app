import React from "react";

import Member from "./GroupMember";

export default function GroupMembers({ members, dispatch }) {
//   const membersArr = Object.entries(members);


//   return membersArr.map((member) => (
//     <Member key={member[0]} role={member[1]} />
//   ));
return null;
}
